package com.senderThread.sender.repository;

import com.senderThread.sender.domain.YercaudPackage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface YercaudPackageRepository extends JpaRepository<YercaudPackage,Long> {
}
