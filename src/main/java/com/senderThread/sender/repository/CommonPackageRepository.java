package com.senderThread.sender.repository;

import com.senderThread.sender.domain.CommonPackage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CommonPackageRepository extends JpaRepository<CommonPackage,Long> {
}
